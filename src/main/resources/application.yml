server:
  port: 2238
spring:
  application:
    name: mysqldoc
  datasource:
    url: jdbc:mysql://localhost:3306/ecom
    username: root
    password: pass@word1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true